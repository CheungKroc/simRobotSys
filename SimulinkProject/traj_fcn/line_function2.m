function [x,y,z] = line_function2(u)
%   the linefuction should be normalized 
%   the parameter u is a variable rangging from 0 to 1
%   a line which is  parallel to the z-axis

    x=0+0*u;
    y=0.4+0*u;
    z=u-0.2;


end

